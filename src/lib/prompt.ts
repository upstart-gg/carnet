import type { Carnet } from '.'

/**
 * Get the prompt for an agent, rewriting it with instructions
 * on how to use its skills and tools.
 */
export function getPrompt(_carnet: Carnet, _agentt: string) {}
